\documentclass[final,num,12pt]{elsarticle}

%% Use the option review to obtain double line spacing
%% \documentclass[authoryear,preprint,review,12pt]{elsarticle}

%% Use the options 1p,twocolumn; 3p; 3p,twocolumn; 5p; or 5p,twocolumn
%% for a journal layout:
%% \documentclass[final,1p,times]{elsarticle}
%% \documentclass[final,1p,times,twocolumn]{elsarticle}
%% \documentclass[final,3p,times]{elsarticle}
%% \documentclass[final,3p,times,twocolumn]{elsarticle}
%% \documentclass[final,5p,times]{elsarticle}
%% \documentclass[final,5p,times,twocolumn]{elsarticle}

%% For including figures, graphicx.sty has been loaded in
%% elsarticle.cls. If you prefer to use the old commands
%% please give \usepackage{epsfig}

%% The amssymb package provides various useful mathematical symbols
\usepackage{amssymb}
%% The amsmath package provides various useful equation environments.
\usepackage{amsmath}
%% The amsthm package provides extended theorem environments
%% \usepackage{amsthm}

%% The lineno packages adds line numbers. Start line numbering with
%% \begin{linenumbers}, end it with \end{linenumbers}. Or switch it on
%% for the whole article with \linenumbers.
\usepackage{lineno}
\usepackage{natbib}
\usepackage{hyperref}
\usepackage{cleveref}
\usepackage{geometry}
\usepackage{todonotes}
\usepackage{enumitem}
\usepackage{caption}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{float}
\usepackage[section]{placeins}
\usepackage{color}
\usepackage{amsmath}
\usepackage{algorithm}%
\usepackage{algorithmicx}%
\usepackage{algpseudocode}%
\usepackage{listings}%
\usepackage{paracol}
\renewcommand{\lstlistingname}{Code}% Listing -> Algorithm
\renewcommand{\lstlistlistingname}{List of \lstlistingname s}% List of Listings 
\setcounter{topnumber}{3}
\setcounter{bottomnumber}{2}
\setcounter{totalnumber}{5}
\renewcommand{\topfraction}{0.9}
\renewcommand{\bottomfraction}{0.8}
\renewcommand{\textfraction}{0.08}
\renewcommand{\floatpagefraction}{0.8}

% MACROS
\renewcommand{\v}{\TextOrMath{\textv}{\mathbf{v}}}
\newcommand{\V}{\mathbf{V}}
\newcommand{\g}{\mathbf{g}}
\newcommand{\x}{\mathbf{x}}
\newcommand{\y}{\mathbf{y}}
\renewcommand{\u}{\TextOrMath{\textu}{\mathbf{u}}}
\renewcommand{\k}{\mathbf{k}}

\renewcommand{\b}{\mathbf{b}}
\newcommand{\n}{\mathbf{n}}
\newcommand{\normal}{\boldsymbol{n}} 

\newcommand{\h}{\boldsymbol{h}} 
\newcommand{\nsigma}{\boldsymbol{n}_\Sigma} 
\newcommand{\vel}{\boldsymbol{v}} 
\newcommand{\fsigma}{\boldsymbol{f}_\Sigma} 
\newcommand{\frad}{\boldsymbol{f}_{rad}} 
\newcommand{\tn}{t^n}
\newcommand{\tnn}{t^{n+1}}
\newcommand{\Sf}{\mathbf{S}_f}

\newcommand\todoin[2][]{\todo[inline, caption={2do}, #1]{\begin{minipage}{\dimexpr\linewidth-4pt\relax}#2\end{minipage}}}

\journal{Computer Physics Communications}

\begin{document}

\begin{frontmatter}

%% Title, authors and addresses

%% use the tnoteref command within \title for footnotes;
%% use the tnotetext command for theassociated footnote;
%% use the fnref command within \author or \affiliation for footnotes;
%% use the fntext command for theassociated footnote;
%% use the corref command within \author for corresponding author footnotes;
%% use the cortext command for theassociated footnote;
%% use the ead command for the email address,
%% and the form \ead[url] for the home page:
%% \title{Title\tnoteref{label1}}
%% \tnotetext[label1]{}
%% \author{Name\corref{cor1}\fnref{label2}}
%% \ead{email address}
%% \ead[url]{home page}
%% \fntext[label2]{}
%% \cortext[cor1]{}
%% \affiliation{organization={},
%%             addressline={},
%%             city={},
%%             postcode={},
%%             state={},
%%             country={}}
%% \fntext[label3]{}

\title{\texttt{acousticHelmholtzFoam}: A parallel finite-volume solver for frequency-domain acoustic waves in heterogeneous two-phase media with PML boundary condition}

%% use optional labels to link authors explicitly to addresses:
%% \author[label1,label2]{}
%% \affiliation[label1]{organization={},
%%             addressline={},
%%             city={},
%%             postcode={},
%%             state={},
%%             country={}}
%%
%% \affiliation[label2]{organization={},
%%             addressline={},
%%             city={},
%%             postcode={},
%%             state={},
%%             country={}}

\author{Chuanchao Xu, Jun Liu, Dieter Bothe, Tomislav Maric} %% Author name

%% Author affiliation
\affiliation{organization={},%Department and Organization
            addressline={}, 
            city={},
            postcode={}, 
            state={},
            country={}}

%% Abstract
\begin{abstract}
%% Text of abstract
\texttt{acousticHelmholtzFoam} is a parallel finite-volume solver for frequency-domain acoustic wave propagation in heterogeneous two-phase media. The solver is implemented in OpenFOAM and solves the complex Helmholtz equation in terms of coupled real and imaginary pressure fields. Perfectly matched layers are used to truncate open domains, and the resulting block-coupled linear system is assembled and solved through PETSc with MUMPS. This paper describes the mathematical model, the finite-volume implementation, the PML field setup.
\end{abstract}

%%Graphical abstract
%\begin{graphicalabstract}
%\includegraphics{grabs}
%\end{graphicalabstract}

%%Research highlights
%\begin{highlights}
%\item Research highlight 1
%\item Research highlight 2
%\end{highlights}

%% Keywords
\begin{keyword}
%% keywords here, in the form: keyword \sep keyword
\texttt{acousticHelmholtzFoam} \sep Helmholtz equation \sep finite volume method \sep two-phase acoustics \sep perfectly matched layer \sep OpenFOAM \sep PETSc

%% PACS codes here, in the form: \PACS code \sep code

%% MSC codes here, in the form: \MSC code \sep code
%% or \MSC[2008] code \sep code (2000 is the default)

\end{keyword}

\end{frontmatter}

%% Add \usepackage{lineno} before \begin{document} and uncomment 
%% following line to enable line numbers
%% \linenumbers

%% main text
%%

\linenumbers
%% Use \section commands to start a section

\section*{Program summary}

\begin{description}[leftmargin=!,labelwidth=4.2cm]
    \item[Program title:] \texttt{acousticHelmholtzFoam}
    \item[Repository:] \todoin{Add public repository or CPC Program Library link.}
    \item[Licensing provisions:] GNU General Public License, version 3 or later.
    \item[Programming language:] C++.
    \item[External dependencies:] OpenFOAM, PETSc, MUMPS, MPI.
    \item[Nature of problem:] Computation of time-harmonic acoustic pressure fields in heterogeneous media and two-phase configurations with open-domain radiation.
    \item[Solution method:] The complex Helmholtz equation is split into real and imaginary pressure components. The resulting block-coupled finite-volume system is assembled in OpenFOAM and solved with PETSc/MUMPS. The companion utility \texttt{setPMLFields} generates the directional PML damping tensor \texttt{sigma}; the solver derives the formulation-specific PML coefficient fields from this tensor.
    \item[Additional comments:] The implementation has been tested with OpenFOAM ESI-v2506 on Linux using MPI parallel execution. The current repository build uses PETSc 3.24.5, OpenMPI support, and direct-solver packages including MUMPS 5.8.1.
\end{description}


\section{Introduction}


\label{sec:intro}

Time-harmonic acoustic fields are commonly described by the Helmholtz equation when the acoustic source operates at a prescribed frequency and the transient wave propagation is not the primary quantity of interest. This frequency-domain formulation is useful in resonators, transducer-driven systems, acoustic radiation problems, and acoustic manipulation devices, because it gives direct access to the pressure amplitude, phase, velocity amplitude, and radiation-pressure-related quantities without resolving many periods of the underlying wave motion \cite{morse1986theoretical,bruus2012acoustofluidicsPT}. In two-phase systems, however, the acoustic problem becomes more demanding: the density and speed of sound may change by orders of magnitude across gas-liquid interfaces, and the pressure field can be strongly affected by reflection, transmission, and resonance at these material jumps.

One motivating application is acoustic levitation of liquid droplets. In single-axis levitators, the standing or partially standing acoustic field determines the radiation pressure and momentum flux acting on the droplet, and therefore influences the droplet position, deformation, and stability \cite{shi1996deformation,yarin1998acoustic,xie2001parametric,andrade2010finite}. Predictive simulations of such systems require an acoustic solver that can represent heterogeneous media and interface-resolved material fields while remaining compatible with two-phase flow solvers. In a finite-volume environment this is particularly attractive, because the same mesh and volume-fraction field used for interface-capturing or interface-tracking methods can also be used to define the acoustic material properties \cite{Brackbill1992,Popinet2018,Liu2023incons}.

Open-domain acoustic simulations also require a robust treatment of outgoing waves. Radiation boundary conditions can be effective in special geometries or for waves with known propagation directions, but they are less convenient for general three-dimensional fields and near-field radiation problems. Perfectly matched layers (PMLs) provide a more flexible domain-truncation strategy by attenuating outgoing waves in an artificial absorbing layer while minimizing reflections at the interface between the physical domain and the layer \cite{Kaltenbacher2013modified}. For a finite-volume implementation in OpenFOAM, the PML formulation must be expressed in terms of operators and coefficient fields that can be assembled efficiently on arbitrary meshes.

This paper presents \texttt{acousticHelmholtzFoam}, a parallel OpenFOAM solver for frequency-domain acoustic waves in heterogeneous two-phase media. The complex Helmholtz equation is split into coupled real and imaginary pressure equations, producing a real-valued block system. The finite-volume operators are assembled in OpenFOAM, while the global block-coupled linear system is solved using PETSc with MUMPS. A companion utility, \texttt{setPMLFields}, generates the directional PML damping tensor \texttt{sigma} for rectangular and spherical absorbing layers. The solver then derives the scalar and tensor coefficient fields required by the frequency-domain formulation. The solver also computes derived fields such as acoustic velocity components, pressure amplitude, radiation pressure, and momentum flux, which are useful for acoustic levitation and related multiphysics applications.

The paper is organized as follows. \cref{sec:model} derives the acoustic model and PML formulation. \cref{sec:implementation} describes the finite-volume implementation and block-coupled solution strategy. \cref{sec:verification} presents verification cases, while \cref{sec:application} outlines the intended application to single-axis acoustic levitation.


\section{Mathematical model}
\label{sec:model}

\subsection{Wave equation for heterogeneous media}
The governing equations of fluid motion for density $\rho$, pressure $p$, and velocity $\v$ are expressed as follows:
\begin{itemize}
\item The mass conservation equation:
        \begin{equation}
            \frac{\partial \rho}{\partial t} + \nabla\cdot(\rho \v)=0.
            \label{eq:massConserv}
        \end{equation}
        \item The momentum conservation equation (neglecting gravitational/viscous effects):
        \begin{equation}
            \frac{\partial \rho \v}{\partial t} + \nabla\cdot(\rho \v\v)=-\nabla p.
            \label{eq:momConserv}    
        \end{equation}
        \item The equation of state
        \begin{equation}
            \rho  =\rho(p,s),
        \end{equation}
        with the specific entropy $s$.
\end{itemize}  
   
Consider a quiescent flow ($\v_0=\mathbf{0}$) characterized by a spatially dependent density $\rho_0(\x)$ prior to the introduction of any acoustic disturbance. Classical perturbation theory is employed to describe the propagation of acoustic waves, wherein the acoustic field is represented as small perturbations (denoted by the subscript $1$, with $|\rho_1|\ll \rho_0$ and $|p_1|\ll p_0$) in the density, pressure, and velocity fields:
    \begin{equation}
        \begin{aligned}
            \rho &= \rho_0 + \rho_1, \\
            p &= p_0 + p_1, \\
            \v &= \v_1.
            \label{eq:1stPert}
        \end{aligned}
    \end{equation}

For the unperturbed fluid, $\v=0$, and it follows from \cref{eq:momConserv} that $\nabla p_0=0$.
    
Substituting \cref{eq:1stPert} into \cref{eq:momConserv} and neglecting higher-order terms (e.g., $\rho_1\v_1$, $\v_1\v_1$) yields
        \begin{equation}
            \rho_0 \frac{\partial \v_1}{\partial t}=-\nabla p_1.
            \label{eq:1stMomConserv}            
        \end{equation}

After introducing the total derivative $\frac{d}{dt}:=\frac{\partial}{\partial t} + \v \cdot \nabla$, \cref{eq:massConserv} can be written as
\begin{equation}
    \frac{d \rho}{dt} + \rho \nabla\cdot\v =0 
\end{equation}

Since sound propagation is an adiabatic process, $\frac{d s}{d t}=0$, and we have
\begin{equation}
     \frac{d \rho}{dt} =\left( \frac{\partial \rho}{\partial p}\right)_s\frac{d p}{\partial t} + \left(\frac{\partial \rho}{\partial s}\right)_p\frac{d s}{d t} = \frac{\partial \rho}{\partial p}\frac{d p}{\partial t} = \frac{1}{c^2}\left(\frac{\partial p}{\partial t} + (\v\cdot\nabla)p\right)
\end{equation}
where we have used the definition of the sound speed $c:=\sqrt{\frac{\partial p}{\partial \rho}}$. The mass conservation equation now becomes
\begin{equation}
    \frac{\partial p}{\partial t} + (\v\cdot\nabla)p + \rho c^2 \, \nabla\cdot\v = 0
    \label{eq:pMassConti}
\end{equation}

Following the same procedure, we use the first-order perturbation ansatz \cref{eq:1stPert}. Neglecting higher-order terms gives
\begin{equation}
    \frac{\partial p_1}{\partial t} = -\rho_0 c^2\, \nabla \cdot \v_1.
    \label{eq:1stMassConserv}
\end{equation}
Taking the time derivative and then using \cref{eq:1stMomConserv} to eliminate $\v_1$ gives
\begin{equation}
    \frac{\partial^2 p_1}{\partial t^2}  = -\rho_0 c^2\, \nabla \cdot \frac{\partial \v_1}{ \partial t} =\rho_0 c^2\, \nabla \cdot\left(\frac{\nabla p_1}{\rho_0}\right),
    \label{eq:heteWaveP}
\end{equation}
which is the pressure wave equation for a heterogeneous medium. Taking $\rho_0$ as constant gives the familiar form of the wave equation for a homogeneous medium:
\begin{equation}
    \frac{\partial^2 p_1}{\partial t^2} = c^2 \Delta p_1
\end{equation}

\subsection{Helmholtz equation}
If the acoustic system is driven at a single frequency and the timescale is much larger than the period of the sound wave, the acoustic pressure and velocity fields can be written in the following time-harmonic form:
\begin{equation}
    \begin{aligned}
        p_1(x,t) &= \mathrm{Re}(e^{-i\omega t}P(x)),\\
        \v_1(x,t) &= \mathrm{Re}(e^{-i\omega t}\V(x)).
    \end{aligned}
    \label{as:timeHarmonic}
\end{equation}
where the complex pressure $P(x)=P_{re}+ iP_{im}(x)$ is a spatial function.
Inserting \cref{as:timeHarmonic} into the first-order momentum equation \cref{eq:1stMomConserv} gives
\begin{equation}
    i\omega\rho_0 \V = -\nabla P
\end{equation}
Substituting \cref{as:timeHarmonic} into the general wave equation \cref{eq:heteWaveP} then gives
\begin{equation}
    \rho_0\nabla\cdot\left(\frac{\nabla P}{\rho_0}\right) + k^2P=0,
    \label{eq:heteHelmholtz}
\end{equation}
where $k=\frac{\omega}{c}$ is the wave number. 


\subsection{Acoustic radiation pressure}
One major motivation for developing the frequency-domain solver is the
simulation of droplets in single-axis acoustic levitators. The time-averaged
force that supports the droplet results from the acoustic radiation pressure,
whose derivation requires a second-order perturbation expansion:
\begin{equation}
    \begin{aligned}
        \rho &= \rho_0 + \rho_1 + \rho_2, \\
        p &= p_0 + p_1 + p_2, \\
        \v &= \v_1 + \v_2.
        \label{eq:2stPert}
    \end{aligned}
\end{equation}
Substituting this expansion into the momentum equation
\cref{eq:momConserv}, neglecting terms above second order, and using the
first-order momentum equation \cref{eq:1stMomConserv} gives
\begin{equation}
    \nabla p_2 =
    -\rho_0\frac{\partial \v_2}{\partial t}
    -\frac{\partial (\rho_1\v_1)}{\partial t}
    -\rho_0\nabla\cdot(\v_1\v_1).
\end{equation}
Assuming constant unperturbed material properties within each medium,
expanding $\partial(\rho_1\v_1)/\partial t$, and using the first-order
momentum and mass equations \cref{eq:1stMomConserv,eq:1stMassConserv} yields
\begin{equation}
    \nabla p_2 =
    -\rho_0\frac{\partial \v_2}{\partial t}
    +\frac{\nabla p_1^2}{2c^2\rho_0}
    -\rho_0(\nabla\v_1)\cdot\v_1.
\end{equation}
Here the isentropic relation $p_1=c^2\rho_1$ has been used. For an
irrotational first-order velocity field, $\v_1=\nabla\Phi$, the identity
$(\nabla\v_1)\cdot\v_1=\frac{1}{2}\nabla|\v_1|^2$ applies. Moreover, the
time average of $\partial\v_2/\partial t$ over one driving period vanishes.
Denoting the time average by $\langle\cdot\rangle:= \frac{1}{T}\int_0^T \cdot \, dt$ therefore gives
\begin{equation}
    \langle p_2\rangle =
    \frac{\langle p_1^2\rangle}{2\rho_0c^2}
    -\frac{\rho_0}{2}\langle|\v_1|^2\rangle.
\end{equation}
Finally, applying the time-harmonic ansatz \cref{as:timeHarmonic} gives
\begin{equation}
    \langle p_2\rangle =
    \frac{\langle |e^{-\omega t}P(x)|^2\rangle}{2\rho_0c^2}
    -\frac{\rho_0}{2}\langle|e^{-\omega t}\V(x)|^2\rangle =  \frac{ |P(x)|^2}{4\omega\rho_0c^2}
    -\frac{\rho_0}{4\omega}|\V(x)|^2.
    \label{eq:acousticRadiationPressure}
\end{equation}
Thus, the acoustic radiation pressure $p_{\mathrm{rad}}:=\langle p_2\rangle$
can be evaluated from the first-order pressure and velocity fields computed by
the frequency-domain solver.


\subsection{Perfectly matched layer}
As the simulation domain must be closed, open acoustic systems, such as sound waves emitted by a speaker and propagating into the far field, require special treatment. For this reason, the open boundary must be replaced by a boundary condition that allows the simulation domain to be enclosed. If the outgoing wave has a specific form or fixed direction, an absorbing or radiation boundary condition can be a simple way to model the open boundary. For more general cases, the perfectly matched layer (PML) is one of the most effective approaches.

With a PML, the simulation domain is extended by an additional layer at the open boundary. Through the modification of the Helmholtz equation, outgoing waves enter this layer without reflection and are fully absorbed inside it. This makes the truncated domain behave like an infinitely extended region.

 The Helmholtz equation is modified by extending the real spatial coordinate $\x$ into a complex space, defined as $\tilde{\x} = \x + i\h(\x)$. Under this transformation, \cref{eq:heteHelmholtz} becomes
\begin{equation}
               \rho_0 \tilde\nabla\cdot\left(\frac{\tilde\nabla P}{\rho_0}\right) + k^2P=0, 
              \label{eq:stretchedHelmholtz}
\end{equation}
where $\tilde{\nabla}$ denotes the Laplacian operator in the stretched coordinate system. To illustrate the auxiliary function $h(x)$, we consider a domain with constant $\rho_0$. The corresponding plane traveling wave solution to \cref{eq:stretchedHelmholtz} is
 \begin{equation*}
   P = |P| \exp(i\k \cdot \tilde \x)=|P| \exp(-\k \cdot \h(\x)) \exp(i\k\cdot \x).  
 \end{equation*}
 Here, the wave vector $\k$ has the direction of the travelling wave and a magnitude equal to the wave number. $\h(\x)$ is designed such that it introduces no artificial damping within the interior region while providing attenuation in the PML region, which means
\begin{equation}
            \k\cdot \h(\x) = \left\{
            \begin{array}{ll}
                > 0 & x \in \Omega^{PML} \\
                = 0 & x \in \Omega / \Omega^{PML}
            \end{array}
            \right.
            \label{eq:planeWaveDamp}
\end{equation}
 
 Building upon the fundamental principles of the PML formulation described above, we derive here the specific implementation proposed by Kaltenbacher et al.\cite{Kaltenbacher2013modified}. Following \citep{Kaltenbacher2013modified}, the complex coordinate transformation is defined as $i\h(\x) = \frac{1}{i\omega}\int \boldsymbol{\sigma}(\x) \,dx$, where $\boldsymbol{\sigma}(\x)$ is the damping vector composed of $\sigma_x(x)$, $\sigma_y(y)$, and $\sigma_z(z)$, whose values are negative to satisfy \cref{eq:planeWaveDamp}. Accordingly, the following relations hold:
 \begin{equation}
     \begin{aligned}
         \frac{\partial \tilde{x}}{\partial x} & = \eta_x=1+\frac{\sigma_x}{i\omega},\quad  \frac{\partial}{\partial \tilde{x}}=\frac{\rho_0}{\eta_x}\frac{\partial}{\partial x},\\
         \frac{\partial \tilde{y}}{\partial y} & = \eta_y=1+\frac{\sigma_y}{i\omega},\quad  \frac{\partial}{\partial \tilde{y}}=\frac{\rho_0}{\eta_y}\frac{\partial}{\partial y}, \\
         \frac{\partial \tilde{z}}{\partial z} & = \eta_z=1+\frac{\sigma_z}{i\omega},\quad  \frac{\partial}{\partial \tilde{z}}=\frac{\rho_0}{\eta_z}\frac{\partial}{\partial z}. 
     \end{aligned}
     \label{eq:dampEtaDef}
 \end{equation}
 Substituting these expressions into \cref{eq:stretchedHelmholtz} gives
 \begin{equation}
     -\frac{(i\omega)^2}{c^2}P + \frac{\rho_0}{\eta_x}\frac{\partial}{\partial x}\left(\frac{1}{\rho_0 \eta_x}\frac{\partial P}{\partial x}\right) + \frac{\rho_0}{\eta_y}\frac{\partial}{\partial y}\left(\frac{1}{\rho_0 \eta_y}\frac{\partial P}{\partial y}\right) +\frac{\rho_0}{\eta_z}\frac{\partial}{\partial z}\left(\frac{1}{\rho_0 \eta_z}\frac{\partial P}{\partial z}\right)=0.
     \label{eq:extendedStretchHelmholz}
 \end{equation}
 Since $\eta_x$, $\eta_y$, and $\eta_z$ depend solely on $x$, $y$, and $z$, respectively, multiplying both sides of \cref{eq:extendedStretchHelmholz} by $\eta_x\eta_y\eta_z$ leads to the reformulated expression:
 \begin{equation}
     -\eta_x\eta_y\eta_z \frac{(i\omega)^2}{c^2 \rho_0}P + \frac{\partial}{\partial x}\left(\frac{\eta_y\eta_z}{\rho_0\eta_x}\frac{\partial P}{\partial x}\right) + \frac{\partial}{\partial y}\left(\frac{\eta_x\eta_z}{\rho_0\eta_y}\frac{\partial P}{\partial y}\right) +\frac{\partial}{\partial z}\left(\frac{\eta_x\eta_y}{\rho_0\eta_z}\frac{\partial P}{\partial z}\right)=0.
     \label{eq:reformedStretchHelmholz}
 \end{equation}
Expanding the first term using \cref{eq:dampEtaDef} gives 
 \begin{equation}
   \begin{aligned}
      \eta_x\eta_y\eta_z\frac{(i\omega)^2}{c^2}P &= \frac{(i\omega)^2}{c^2}\left(1+\frac{\sigma_x}{i\omega}\right)\left(1+\frac{\sigma_y}{i\omega}\right)\left(1+\frac{\sigma_z}{i\omega}\right)P \\
      &= \frac{1}{c^2}\left((i\omega)^2 + i\omega(\sigma_x+\sigma_y+\sigma_z) + \sigma_x\sigma_y + \sigma_x\sigma_z +\sigma_y\sigma_z + \frac{\sigma_x\sigma_y\sigma_z}{i\omega}\right)P  
 \end{aligned} 
 \label{eq:1stTermExt}
 \end{equation}
 
 We therefore proceed to separate \cref{eq:reformedStretchHelmholz} into its real and imaginary components. 

 Expanding the first term in \cref{eq:dampEtaDef} yields
 \begin{equation}
   \begin{aligned}
      \eta_x\eta_y\eta_z\frac{(i\omega)^2}{c^2}P &= \frac{(i\omega)^2}{c^2}\left(1+\frac{\sigma_x}{i\omega}\right)\left(1+\frac{\sigma_y}{i\omega}\right)\left(1+\frac{\sigma_z}{i\omega}\right)(P_{re} + iP_{im}) \\
      %&= \frac{1}{c^2}\left((i\omega)^2 + i\omega(\sigma_x+\sigma_y+\sigma_z) + \sigma_x\sigma_y + \sigma_x\sigma_z +\sigma_y\sigma_z + \frac{\sigma_x\sigma_y\sigma_z}{i\omega}\right)(P_{re} + iP_{im}) \\
      &=\frac{1}{c^2}
\left[ (AP_{re} - BP_{im})
+ i(AP_{im} + BP_{re}) \right], \quad \text{with} \\
A &= -\omega^2 + \sigma_x\sigma_y + \sigma_x\sigma_z + \sigma_y\sigma_z,\\
B &= \omega(\sigma_x+\sigma_y+\sigma_z) - \frac{\sigma_x\sigma_y\sigma_z}{\omega}
 \end{aligned} 
% \label{eq:1stTermExt}
 \end{equation}
 
Applying the same expansion to the second term in \cref{eq:dampEtaDef} gives
 \begin{equation}
 \begin{aligned}
     \frac{\eta_y\eta_z}{\eta_x}\frac{\partial P}{\partial x} &= \frac{\left(1+\frac{\sigma_y}{i\omega}\right)\left(1+\frac{\sigma_z}{i\omega}\right)}{\left(1+\frac{\sigma_x}{i\omega}\right)}\frac{\partial}{\partial x}(P_{re} + iP_{im}) \\
     &= \left(A\frac{\partial P_{re}}{\partial x} - B\frac{\partial P_{im}}{\partial x}\right)
+ i\left(A\frac{\partial P_{im}}{\partial x} + B\frac{\partial P_{re}}{\partial x}\right) \quad \text{with} \\
A&=1 + \frac{\sigma_x(\sigma_y+\sigma_z -\sigma_x) - \sigma_y\sigma_z }{(\omega^2 + \sigma^2_x)} ,\\
B&= -\frac{(\sigma_x\sigma_y\sigma_z)/\omega + \omega(\sigma_y+\sigma_z-\sigma_x)}{(\omega^2 + \sigma^2_x)}.
    \end{aligned}
 \end{equation}
 The same procedure is applied to the third and fourth terms in \cref{eq:dampEtaDef}. Collecting all contributions yields the PML formulation for wave propagation in the frequency domain:
        \begin{equation*}
            \begin{aligned}
                \frac{\omega^2}{c^2} P_{re}  +  \rho_0 \nabla \cdot\left(\frac{1}{\rho_0} \nabla P_{re}\right) - C_0 P_{re} + C_1 P_{im}   
               + \nabla \cdot (\mathbf{T}_0 \cdot \nabla P_{re} + \mathbf{T}_1 \cdot \nabla P_{im}) &= 0, \\
            \frac{\omega^2}{c^2}P_{im} +  \rho_0 \nabla \cdot\left(\frac{1}{\rho_0} \nabla P_{im}\right)  - C_0 P_{im} - C_1 P_{re}
             + \nabla \cdot (\mathbf{T}_0 \cdot \nabla P_{im} - \mathbf{T}_1 \cdot \nabla P_{re}) &= 0, 
            \end{aligned}
            \label{eq:helmholtzPML}
        \end{equation*}
where $C_0$, $C_1$ and the tensor coefficients $\mathbf{T}_0$, $\mathbf{T}_1$ are defined as follows:
\begin{paracol}{2}
      \begin{equation*}
      %\footnotesize
        \begin{aligned}
            C_0 &= \frac{\sigma_x\sigma_y + \sigma_x\sigma_z + \sigma_y\sigma_z}{c^2} \\%[1ex]
            T_{0xx} &= \frac{\sigma_x(\sigma_y+\sigma_z-\sigma_x) - \sigma_y\sigma_z}{(\omega^2 + \sigma^2_x)}, \\
            T_{0yy} &= \frac{\sigma_y(\sigma_x+\sigma_z-\sigma_y)-\sigma_x\sigma_z}{(\omega^2 + \sigma^2_y)}, \\
            T_{0zz} &= \frac{\sigma_z(\sigma_x+\sigma_y-\sigma_z)-\sigma_x\sigma_y}{(\omega^2 + \sigma^2_z)}, \\
            \mathbf{T_0} &= \begin{pmatrix}
              T_{0xx} & 0 & 0 \\
             0 &  T_{0yy} & 0 \\
             0 & 0 &  T_{0zz}  
            \end{pmatrix} 
        \end{aligned}
    \end{equation*}
    \switchcolumn
       \begin{equation*}
      % \footnotesize
        \begin{aligned}
            C_1 &= \frac{\omega(\sigma_x+\sigma_y+\sigma_z) - (\sigma_x\sigma_y\sigma_z)/\omega}{c^2} \\%[1ex]
            T_{1xx} &= \frac{(\sigma_x\sigma_y\sigma_z)/\omega + \omega(\sigma_y+\sigma_z-\sigma_x)}{(\omega^2 + \sigma^2_x)}, \\
            T_{1yy} &= \frac{(\sigma_x\sigma_y\sigma_z)/\omega + \omega(\sigma_x+\sigma_z-\sigma_y)}{(\omega^2 + \sigma^2_y)}, \\
            T_{1zz} &= \frac{(\sigma_x\sigma_y\sigma_z)/\omega + \omega(\sigma_x+\sigma_y-\sigma_z)}{(\omega^2 + \sigma^2_z)}, \\
            \mathbf{T_1} &= \begin{pmatrix}
              T_{1xx} & 0 & 0 \\
             0 &  T_{1yy} & 0 \\
             0 & 0 &  T_{1zz}   
            \end{pmatrix} 
        \end{aligned}
    \end{equation*}
\end{paracol}
Since medium inhomogeneity is not introduced in the PML, $\rho_0$ in the Laplacian term with the PML coefficient can be canceled out.  
\section{Numerical implementation}
\label{sec:implementation}

\subsection{Solver overview}
\label{sec:solverOverview}
\texttt{acousticHelmholtzFoam} is implemented as an OpenFOAM user application for
frequency-domain acoustic simulations on finite-volume meshes.  The unknown
complex pressure is stored as two real-valued volume fields, \texttt{Pre} and
\texttt{Pim}.  The solver reads the computational mesh, the initial pressure
fields, the material indicator \texttt{alpha.water}, and the acoustic parameters
from \texttt{constant/transportProperties}.  The dictionary provides the gas and
liquid densities \texttt{rhog}, \texttt{rhol}, sound speeds \texttt{cg},
\texttt{cl}, compressibility-related coefficients \texttt{kg}, \texttt{kl}, the
driving frequency \texttt{f}, and the PML parameters.  The PML configuration is
selected through \texttt{PMLType}; the current implementation supports
\texttt{rectangle} and \texttt{spherical} configurations.

The PML damping field is generated before the Helmholtz solve by the companion
utility \texttt{setPMLFields}.  This utility reads the same
\texttt{transportProperties} dictionary and writes only the directional tensor
field \texttt{sigma}, which represents the polynomial damping profile.  The
solver subsequently reads \texttt{sigma} and computes the formulation-specific
fields \texttt{C0}, \texttt{C1}, \texttt{T0}, and \texttt{T1} internally from
the relations given above.  
After the PML damping field has been prepared and the solver-local coefficient
fields have been constructed, \texttt{acousticHelmholtzFoam} forms the
block-coupled system for \texttt{Pim} and \texttt{Pre}.  The diagonal block
contains the variable-density Helmholtz operator, while the off-diagonal blocks
contain the PML coupling terms.  In parallel runs, the finite-volume operators
are assembled on the decomposed OpenFOAM mesh and the processor-interface
couplings are inserted into a distributed PETSc matrix.  The resulting global
linear system is solved with PETSc, using the MUMPS direct solver in the tested
configuration.

The primary outputs are the converged real and imaginary pressure fields.  The
solver also writes derived quantities that are useful for acoustic-force
analysis: the pressure amplitude \texttt{pa}, the real and imaginary velocity
components \texttt{Ure} and \texttt{Uim}, the acoustic radiation-pressure-like
field \texttt{pr}.

\subsection{Finite-volume discretisation}
\label{sec:fvmDiscretisation}
The PML-modified Helmholtz equations are discretised with the cell-centred
finite-volume method implemented in OpenFOAM.  The computational domain is
partitioned into non-overlapping polyhedral control volumes, and each
divergence term is integrated over a control volume and converted into face
fluxes by the Gauss theorem.  The notation follows standard finite-volume
references \cite{Versteeg2007,Ferziger2020}.

The heterogeneous two-phase material properties are represented with the liquid
volume fraction $\alpha$.  The liquid phase has density $\rho_l$ and sound speed
$c_l$, while the gas phase has density $\rho_g$ and sound speed $c_g$.  A
one-field coefficient representation is used,
\begin{equation}
    \begin{aligned}
           \frac{1}{\rho}& = \frac{\alpha}{\rho_l} + \frac{1-\alpha}{\rho_g},  \\
           k^2 &= \omega^2\left(\frac{\alpha}{c_l^2}+\frac{1-\alpha}{c_g^2}\right),
    \end{aligned}
\label{eq:twoPhaseCoefficients}
\end{equation}
where $\alpha=1$ denotes liquid, $\alpha=0$ denotes gas, and intermediate values
occur only in interface cells.  The inverse density is averaged directly because
$1/\rho$ is the coefficient that appears in the variable-density acoustic flux.
At material interfaces, this coefficient is evaluated on faces from the
geometrically reconstructed face fraction $\alpha_f$,
\begin{equation}
    \left(\frac{1}{\rho}\right)_f
    = \frac{\alpha_f}{\rho_l}+\frac{1-\alpha_f}{\rho_g}.
\label{eq:faceInverseDensity}
\end{equation}
Thus the variable-density flux uses an interface-aware face coefficient, while
the wave-number and PML reaction coefficients remain cell-centred.  This gives a
less smeared impedance transition in the flux evaluation and reduces spurious
interface oscillations in derived quantities such as the radiation-pressure
field.

After splitting the complex pressure into real and imaginary components, the
equations are assembled as the block system
\begin{equation}
\begin{bmatrix}
    \hat A & \hat B \\
    -\hat B & \hat A
\end{bmatrix}
\begin{bmatrix}
    P_{re} \\ P_{im} 
\end{bmatrix}
=0,
\label{eq:blockCoupled}
\end{equation}
with the operators
\begin{equation}
    \begin{aligned}
        \hat A & =
        \rho \nabla\cdot\left(\frac{1}{\rho}\nabla\right)
        + \nabla\cdot(\mathbf{T}_0\cdot\nabla)
        + \left(k^2-C_0\right),\\
        \hat B & =
        \nabla\cdot(\mathbf{T}_1\cdot\nabla) + C_1 .
    \end{aligned}
\label{eq:blockOperators}
\end{equation}
The diagonal block $\hat A$ contains the heterogeneous Helmholtz operator and
the real PML contribution.  The off-diagonal block $\hat B$ contains the PML
terms that couple $P_{re}$ and $P_{im}$.  Storing $\mathbf{T}_0$ and
$\mathbf{T}_1$ as diagonal volume-tensor fields preserves the directional
rectangular PML: each Cartesian damping component can vary independently,
including in edge and corner regions.  The tensor Laplacian terms are assembled
with \texttt{fvm::laplacian(T0,P)} and \texttt{fvm::laplacian(T1,P)}.

For a cell $P$ with volume $V_P$ and boundary faces $f$, the discrete diagonal
operator applied to a pressure component has the form
\begin{equation}
\begin{aligned}
    (\hat A_h P)_P ={}&
    \rho_P \sum_{f\in\partial V_P}
        \left(\frac{1}{\rho}\right)_f
        (\nabla P)_f\cdot\Sf
    + \sum_{f\in\partial V_P}
        \left(\mathbf{T}_{0,f}\cdot(\nabla P)_f\right)\cdot\Sf \\
    &+ (k_P^2-C_{0,P})P_P V_P ,
\end{aligned}
\label{eq:discreteDiagonalOperator}
\end{equation}
where $\Sf$ is the outward-pointing face-area vector.  The corresponding
real--imaginary coupling operator is
\begin{equation}
    (\hat B_h P)_P =
    \sum_{f\in\partial V_P}
        \left(\mathbf{T}_{1,f}\cdot(\nabla P)_f\right)\cdot\Sf
    + C_{1,P}P_P V_P .
\label{eq:discreteCouplingOperator}
\end{equation}
The reaction terms are treated implicitly.  The Laplacian terms are assembled
using linear face interpolation and the selected OpenFOAM face-normal gradient
scheme.  On orthogonal meshes, the normal gradient at an internal face shared
by cells $P$ and $N$ is approximated by a centred difference.  For
non-orthogonal meshes, OpenFOAM's corrected Laplacian scheme can be selected;
its explicit non-orthogonal contribution is updated through the non-orthogonal
correction loop.

OpenFOAM first assembles each scalar operator into its lower-diagonal-upper
matrix representation, including physical-boundary contributions.  The
diagonal operators and the two PML coupling operators are then inserted into
the corresponding blocks of the global PETSc matrix.  In parallel, couplings
across processor patches are inserted explicitly using global cell indices, so
the distributed PETSc system represents the same finite-volume stencil as the
undecomposed mesh.  The resulting block system is solved with PETSc/MUMPS.

\subsection{PML configuration}
The PML damping profile is generated by the utility \texttt{setPMLFields}.  The
utility reads \texttt{constant/transportProperties}, evaluates the damping at
cell centres and boundary-face centres, and writes the diagonal tensor field
\texttt{sigma}.  Empty, wedge, and processor patches are not assigned a physical
boundary damping value.  The Helmholtz solver then reads \texttt{sigma} and
constructs the formulation-specific fields \texttt{C0}, \texttt{C1},
\texttt{T0}, and \texttt{T1}; these fields are therefore not inputs to
\texttt{setPMLFields}.

The common profile parameters are the PML thickness \texttt{L}, the polynomial
order \texttt{po}, and the peak damping strength.  The entry \texttt{sigmaMax}
defines a scalar peak value.  For directional rectangular PMLs it can be
overridden component-wise by \texttt{sigmaMaxVector}; if this vector is absent,
all three components use \texttt{sigmaMax}.  The current implementation supports
two geometries:
\begin{itemize}
    \item \texttt{PMLType rectangle}: the user supplies the outer coordinate
    bounds \texttt{PMLMax} and \texttt{PMLMin}.  For each Cartesian component
    $i$, the distance $d_i$ to the nearest corresponding bound is used to define
    \begin{equation}
        \sigma_i
        =
        -\sigma_{\max,i}
        \left[
        \min\left(\max\left(\frac{L-d_i}{L},0\right),1\right)
        \right]^{p_o}.
    \end{equation}
    This produces a diagonal tensor
    $\boldsymbol{\sigma}=\mathrm{diag}(\sigma_x,\sigma_y,\sigma_z)$ and allows,
    for example, a wedge levitator case to damp only the $x$ and $y$ directions
    by setting the $z$ component of \texttt{sigmaMaxVector} to zero.

    \item \texttt{PMLType spherical}: the user supplies \texttt{origin},
    \texttt{Rmax}, and \texttt{L}.  With $r=|\mathbf{x}-\mathbf{x}_0|$, the
    scalar profile is
    \begin{equation}
        \sigma
        =
        -\sigma_{\max}
        \left[
        \min\left(\max\left(\frac{L-(R_{\max}-r)}{L},0\right),1\right)
        \right]^{p_o},
    \end{equation}
    and the utility writes
    $\boldsymbol{\sigma}=\mathrm{diag}(\sigma,\sigma,\sigma)$.  Some cases define
    an auxiliary \texttt{Rmin} entry and set \texttt{Rmax = Rmin + L}; however,
    \texttt{setPMLFields} uses \texttt{Rmax}, \texttt{origin}, and \texttt{L}
    directly.
\end{itemize}

The utility does not infer the absorbing layer from boundary names.  The PML
extent and damping strength must therefore be chosen consistently with the mesh,
wavelength.  Excessive damping over too
few cells can still introduce numerical reflection.

\section{Verification}
\label{sec:verification}

\subsection{Error norms and convergence assessment}
For verification cases with an analytical reference, the numerical quantity
$y_h(\x_i)$ is compared with the corresponding exact value
$y_{\mathrm{ex}}(\x_i)$ at a common set of sample points $\x_i$.  The relative
discrete $L_2$ and $L_\infty$ errors are defined as
\begin{equation}
    E_2 =
    \frac{
    \left(\sum_i |y_h(\x_i)-y_{\mathrm{ex}}(\x_i)|^2\right)^{1/2}
    }{
    \left(\sum_i |y_{\mathrm{ex}}(\x_i)|^2\right)^{1/2}
    },
    \qquad
    E_\infty =
    \frac{
    \max_i |y_h(\x_i)-y_{\mathrm{ex}}(\x_i)|
    }{
    \max_i |y_{\mathrm{ex}}(\x_i)|
    }.
    \label{eq:verificationErrorNorms}
\end{equation}
For two successive mesh resolutions with characteristic cell sizes $h_1$ and
$h_2$, the observed order of convergence is estimated from either error norm
as
\begin{equation}
    q=\frac{\log(E_{h_1}/E_{h_2})}{\log(h_1/h_2)}.
    \label{eq:verificationObservedOrder}
\end{equation}
The case-specific definitions and normalization of $y_h$ and
$y_{\mathrm{ex}}$ are stated in the corresponding subsections below.

\subsection{Piston radiation}
The baffled-piston radiation problem is used as a verification case for the
frequency-domain solver because it has a closed-form analytical reference for
both the on-axis near field and the far-field directivity.  The axisymmetric
configuration is shown in \cref{fig:piston}.  A rigid circular piston of radius
$a$ is mounted in an infinite rigid baffle and oscillates harmonically in the
normal direction with velocity amplitude $u_0$.  The physical acoustic domain
is the half-space above the baffle, truncated numerically by a spherical PML
between the radii $R_{\min}$ and $R_{\max}$.
\begin{figure}[H]
    \centering
    \begin{tikzpicture}[x=1cm,y=1cm,>=latex]
        \def\rmin{3.0}
        \def\rmax{4.2}
        \def\piston{1.15}
        \def\pistony{0.05}

        % Computational domain and spherical PML.
        \fill[blue!6] (0,0) -- (0:\rmin) arc (0:180:\rmin) -- cycle;
        \fill[gray!18] (0:\rmin) arc (0:180:\rmin)
            -- (180:\rmax) arc (180:0:\rmax) -- cycle;

        \draw[thick] (-\rmax,0) -- (\rmax,0);
        \draw[thick] (0:\rmin) arc (0:180:\rmin);
        \draw[thick] (0:\rmax) arc (0:180:\rmax);
        \draw[->,dashed] (0,0) -- (25:\rmin)
            node[pos=0.63,below right] {$R_{\min}$};
        \draw[->,dashed] (0,0) -- (120:\rmax)
            node[pos=0.72,left] {$R_{\max}$};

        % Piston mounted in the infinite baffle.
        \draw[line width=1.3pt] (-\piston,\pistony) -- (\piston,\pistony);
        \draw[<->] (-\piston,-0.55) -- (\piston,-0.55)
            node[midway,below] {$2a$};
        \draw[->,thick] (0,\pistony) -- (0,0.78)
            node[right] {$u_0 e^{-i\omega t}$};
        \node[below] at (0,-0.05) {piston};


        \node at (0,1.55) {simulation domain};
        \node[rotate=0] at (2.65,2.35) {PML};
        \node[above] at (-2.8,0.02) {infinite baffle};
    \end{tikzpicture}
    \caption{Axisymmetric baffled-piston configuration used for verification.
    The piston drives the physical acoustic domain, and outgoing waves are
    absorbed in the spherical PML surrounding the domain.}
    \label{fig:piston}
\end{figure}
The reference pressure follows from the Rayleigh integral over the piston
surface.  Each differential surface element is treated as a monopole source,
and the pressure at an observation point is obtained by integrating these
contributions over the piston face,
\begin{equation}
    p(\mathbf{r},t)=i\omega \rho_0 u_0\int_{S}\frac{e^{i(\omega t-kr')}}{2\pi r'}dS.
\end{equation}
Here $\omega$ is the angular frequency, $k=\omega/c$ is the wave number,
$S$ is the piston surface, and $r'$ is the distance between the observation
point and the running integration point on the piston.  The gas density and
sound speed are denoted by $\rho_0$ and $c$, respectively.  In the numerical
case, the open boundary is represented by the spherical PML sketched in
\cref{fig:piston}.  Its damping profile is controlled by the polynomial order
$po$ and reaches the prescribed maximum value $\sigma_{\max}$ at the outer PML
boundary.

The radiated pressure is examined in two complementary regimes.  In the near
field, a simple analytical expression is available on the symmetry axis, where
the pressure magnitude can be expressed with spherical coordinate
\begin{equation}
    |p|(r,\theta=0) = 2\rho_0cu_0 \left| \sin\left\{ \frac{1}{2}kr\left[\sqrt{1+\left(\frac{a}{r}\right)^2}-1\right] \right\} \right|
    \label{fc:nearField}
\end{equation}
and is therefore suitable for a pointwise comparison inside the computational
domain.  In the far field, where $r\gg a$, the analytical solution is written
in spherical coordinates as
\begin{equation}
    p(r,\theta,t)= \frac{i\omega a ^2\rho_0u_0}{2r}e^{i(\omega t-kr)}\left[\frac{2J_1(ka\sin\theta)}{ka\sin\theta}\right].
    \label{fc:farField}
\end{equation}
Here $J_1$ is the first-order Bessel function.  The term in brackets gives the
angular directivity of the piston and provides an independent check of the
computed complex pressure field.

The parameters used in the numerical case are listed in \cref{tab:paramsPiston}.
They specify the acoustic medium, piston excitation, and spherical PML shown in
\cref{fig:piston}.

\begin{table}[H]
    \centering
    \caption{Simulation parameters for the baffled-piston radiation case.}
    \label{tab:paramsPiston}
    \begin{tabular}{llcl}
        \toprule
        Quantity & Symbol & Value & Unit \\
        \midrule
        Gas density & $\rho_0$ & 1.2 & $\mathrm{kg\,m^{-3}}$ \\
        Sound speed & $c$ & 343 & $\mathrm{m\,s^{-1}}$ \\
        Piston velocity amplitude & $u_0$ & 1.0 & $\mathrm{m\,s^{-1}}$ \\
        Driving frequency & $f$ & 10.0 & $\mathrm{kHz}$ \\
        Acoustic wavelength & $\lambda=c/f$ & $3.43\times10^{-2}$ & $\mathrm{m}$ \\
        Piston radius & $a$ & 0.10 & $\mathrm{m}$ \\
        PML inner radius & $R_{\min}$ & 0.20 & $\mathrm{m}$ \\
        PML outer radius & $R_{\max}$ & 0.28 & $\mathrm{m}$ \\
        Maximum PML damping & $\sigma_{\max}$ & $1.0\times10^5$ & $\mathrm{s^{-1}}$ \\
        PML polynomial order & $po$ & 3 & -- \\
        \bottomrule
    \end{tabular}
\end{table}

\subsubsection{Mesh-convergence post-processing}
Mesh convergence is assessed using 20, 50, and 100 cells per wavelength.  For
the near-field test, the computed pressure is sampled on the symmetry axis and
compared directly with \cref{fc:nearField}.  The numerical pressure amplitude is
evaluated from the computed complex pressure as
\begin{equation}
    |p|_n(\x_i)=\sqrt{P_{re}^2(\x_i)+P_{im}^2(\x_i)} ,
\end{equation}
and compared with the analytical amplitude $p_{\mathrm{ex}}(\x_i)$ at sampled
points $\x_i$ on the symmetry axis. For this case, the quantities used in
\cref{eq:verificationErrorNorms} are normalized by the characteristic pressure
$p_0=\rho_0 c u_0$.
The resulting errors and observed orders are listed in
\cref{tab:pistonConvergence}.  The corresponding pointwise comparison is shown
in \cref{fig:pistonNearAmp}: the numerical curves reproduce the oscillatory
near-field structure and approach the analytical amplitude as the mesh is
refined.

\input{tables/nearField_convergence_table.tex}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{figures/piston_nearField_onAxis_meshConvergence.png}%
    \caption{Piston radiation: comparison of the near-field on-axis pressure
    amplitude with the analytical baffled-piston solution.}
    \label{fig:pistonNearAmp}
\end{figure}


The far-field test evaluates the angular directivity at the fixed radius
$R=4\,\mathrm{m}$ for polar angles between $0^\circ$ and $90^\circ$.  This
radius is far outside the computational domain and would be expensive to mesh
directly.  Instead, the far-field pressure is reconstructed from the simulated
complex pressure on the spherical surface at $r=0.2\,\mathrm{m}$ using the
boundary integral
\begin{equation}                   
        |p|_n(R,\theta) = \left|\int\limits_{\partial B_{r=0.2}(0)} \left( P\frac{\partial G}{\partial n }-G\frac{\partial P}{\partial n } \right)\, \mathrm{d}S\right|.
\end{equation}
Here $G(x,\tilde{x})=\frac{e^{ik|x-\tilde{x}|}}{4\pi|x-\tilde{x}| }$ is the
free-space Green function and the complex pressure $P(x) = P_{re}+iP_{im}$ .  The resulting directivity is compared with the
analytical far-field expression in \cref{fig:pistonFarAmp}.  The polar plot
uses a logarithmic pressure scale to make both the main lobe and the angular
decay visible; the numerical pattern follows the analytical profile closely,
with the largest differences occurring near the piston plane at
$\theta=90^\circ$.


\begin{figure}[H]
    \centering
    \includegraphics[width=0.58\textwidth]{figures/piston_farFieldPatternPolar.png}%
    \caption{Piston radiation: far-field directivity reconstructed at
    $R=4\,\mathrm{m}$ and compared with the analytical baffled-piston pattern.}
    \label{fig:pistonFarAmp}
\end{figure}

\input{tables/farField_convergence_table.tex}

The convergence trends from the near-field amplitude and far-field directivity
studies are summarized in \cref{fig:pistonMetricsConvergence}.  This figure
collects the normalized error measures from \cref{tab:pistonConvergence} and
\cref{tab:pistonFarFieldConvergence}, making clear that the on-axis near-field
amplitude improves strongly with mesh refinement, while the far-field
directivity error is smaller and varies more weakly over the tested mesh range.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{figures/piston_metrics_meshConvergence.png}%
    \caption{Piston radiation: Mesh-convergence metrics for the near-field pressure amplitude and far-field directivity.}
    \label{fig:pistonMetricsConvergence}
\end{figure}

\FloatBarrier
\subsection{One-dimensional layered interface}
The layered-interface case verifies both wave transmission across a strong
material-property jump and attenuation by the directional tensor PML.  Its
configuration is shown in \cref{fig:1DLayerd}.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[x=1cm,y=1cm,>=latex]
        \def\xzero{0}
        \def\xone{3.0}
        \def\xtwo{6.5}
        \def\xthree{9.0}
        \def\h{1.15}

        \fill[blue!10] (\xzero,0) rectangle (\xone,\h);
        \fill[green!12] (\xone,0) rectangle (\xtwo,\h);
        \fill[red!12] (\xtwo,0) rectangle (\xthree,\h);

        \draw[thick] (\xzero,0) rectangle (\xthree,\h);
        \draw[thick,dashed] (\xone,0) -- (\xone,\h);
        \draw[thick,dashed] (\xtwo,0) -- (\xtwo,\h);

        \foreach \yy in {0.15,0.35,0.55,0.75,0.95}
        {
            \draw[thick] (-0.28,\yy) -- (\xzero,\yy);
        }
        \draw[very thick] (\xzero,-0.08) -- (\xzero,\h+0.08);
        \draw[->,thick] (-0.7,0.58) -- (-0.15,0.58)
            node[midway,above] {$u_0 e^{i\omega t}$};

        \node at (1.5,0.58) {medium 1};
        \node at (1.5,0.23) {$\rho_1,\ c_1$};
        \node at (4.75,0.58) {medium 2};
        \node at (4.75,0.23) {$\rho_2,\ c_2$};
        \node at (7.75,0.58) {PML};
        \node at (7.75,0.23) {$\sigma(x)$};

        \draw[->,thick] (\xzero,-0.55) -- (9.55,-0.55) node[right] {$x$};
        \foreach \x/\lab in {\xzero/$0$,\xone/$L_1$,\xtwo/$L_2$,\xthree/$L$}
        {
            \draw (\x,-0.47) -- (\x,-0.65);
            \node[below] at (\x,-0.65) {\lab};
        }

    \end{tikzpicture}
    \caption{Configuration of the one-dimensional layered-domain validation case.}
    \label{fig:1DLayerd}
\end{figure}

The transducer at the left boundary vibrates harmonically with frequency $f$
and velocity amplitude $u_0$.  The emitted wave travels through medium~1 in
$0<x<L_1$, crosses the material interface into medium~2 in $L_1<x<L_2$,
and finally enters the PML in $L_2<x<L$.

The analytical solution reads
\begin{equation}
    P(x) = 
    \begin{cases}
        \rho_1c_1u_0\frac{e^{ik_1x}+Re^{ik_1(2L_1-x)}}{1-Re^{i2k_1L_1}} & 0<x<L_1, \\[1ex]
        \rho_1c_1u_0\frac{1+R}{1-Re^{i2k_1L_1}}e^{i\left[k_2(x-L_1)+k_1L_1\right]} & L_1<x<L_2, \\[1ex]
        e^{\frac{-\sigma_{\max}(L-L_2)}{(p+1)c_2}\left(\frac{x-L_2}{L-L_2}\right)^{p+1}}
        \rho_1c_1u_0\frac{1+R}{1-Re^{i2k_1L_1}}e^{i\left[k_2(x-L_1)+k_1L_1\right]} & L_2<x<L .
    \end{cases}
\end{equation}
Here $k_j=\omega/c_j$ and
$R=(\rho_2c_2-\rho_1c_1)/(\rho_2c_2+\rho_1c_1)$.  The simulation parameters
are listed in \cref{tab:params1DTwoPhase}.
\begin{table}[H]
    \centering
    \caption{Simulation parameters for the one-dimensional layered-interface case.}
    \label{tab:params1DTwoPhase}
    \begin{tabular}{llcl}
        \toprule
        Quantity & Symbol & Value & Unit \\
        \midrule
        Medium-1 density & $\rho_1$ & 1.2 & $\mathrm{kg\,m^{-3}}$ \\
        Medium-2 density & $\rho_2$ & 1000 & $\mathrm{kg\,m^{-3}}$ \\
        Medium-1 sound speed & $c_1$ & 343 & $\mathrm{m\,s^{-1}}$ \\
        Medium-2 sound speed & $c_2$ & 1500 & $\mathrm{m\,s^{-1}}$ \\
        Transducer velocity amplitude & $u_0$ & 0.01 & $\mathrm{m\,s^{-1}}$ \\
        Driving frequency & $f$ & 20 & $\mathrm{kHz}$ \\
        Domain length & $L$ & 0.35 & $\mathrm{m}$ \\
        Interface position & $L_1$ & 0.09 & $\mathrm{m}$ \\
        PML start position & $L_2$ & 0.20 & $\mathrm{m}$ \\
        PML thickness & $L-L_2$ & 0.15 & $\mathrm{m}$ \\
        Maximum PML damping & $\sigma_{\max}$ & $5.0\times10^5$ & $\mathrm{s^{-1}}$ \\
        PML polynomial order & $p$ & 3 & -- \\
        Number of cells in $x$ & $N_x$ & 8000 & -- \\
        \bottomrule
    \end{tabular}
\end{table}

The numerical real and imaginary pressure components agree closely with the
analytical solution throughout both the physical domain and the PML, as shown
in \cref{fig:1DLayerdCompare}.  In the physical domain, the relative $L_2$
errors are $1.24\times10^{-3}$ and $1.54\times10^{-3}$ for
$P_{\mathrm{re}}$ and $P_{\mathrm{im}}$, respectively.  Because the acoustic
impedance $\rho c$ of water is much larger than that of air, $R\approx1$ and
the field in medium~1 is approximately a standing wave.  In this limit, the
medium-1 solution can be written as
\begin{equation}
    P(x)=i\rho_1c_1u_0\frac{\cos[k_1(x-L_1)]}{\sin(k_1L_1)},
\end{equation}
which has only an imaginary component.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{figures/pressureField_Pre_Pim_compare.png}%
    \caption{Directional tensor PML validation: comparison of the numerical real and imaginary pressure components with the analytical layered-interface solution.  Markers identify the numerical samples, while the solid black curves show the analytical reference.}
    \label{fig:1DLayerdCompare}
\end{figure}

At the interface, the outgoing wave is transmitted into medium~2.  Since the
right boundary represents an open domain, no physical reflection should return
from that side.  The field in the undamped part of medium~2,
$L_1<x<L_2$, is therefore a plane travelling wave with constant pressure
amplitude.  Upon entering the PML, the complex coordinate stretching multiplies
the wave amplitude by the attenuation factor
$\exp[-\mathbf{k}\cdot\mathbf{h}(x)]$.  For the polynomial damping profile used
in this case, this factor is
\begin{equation}
    \exp\left[
        \frac{-\sigma_{\max}(L-L_2)}{(p+1)c_2}
        \left(\frac{x-L_2}{L-L_2}\right)^{p+1}
    \right],
    \qquad L_2<x<L,
\end{equation}
Consequently, the amplitude remains constant before
the PML and then decreases smoothly towards the truncated boundary, as
confirmed by \cref{fig:1DLayerdAmplitude}.  Over $0.20<x<0.35\,\mathrm{m}$,
the numerical and analytical complex pressures differ by only
$1.30\times10^{-3}$ in relative $L_2$ norm, and their pressure amplitudes
differ by $1.01\times10^{-5}$.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{figures/pressureField_abs_compare.png}%
    \caption{Directional tensor PML validation: numerical and analytical pressure amplitude.  The transmitted plane wave has constant amplitude in medium~2 before the PML and is smoothly attenuated inside the PML.}
    \label{fig:1DLayerdAmplitude}
\end{figure}

\FloatBarrier
\subsubsection{PML damping sensitivity}
The one-dimensional layered-interface case is also used to examine the influence of the PML damping strength.  All physical parameters, the mesh, the PML thickness, and the polynomial order are kept fixed, while only the maximum damping coefficient $\sigma_{\max}$ is varied. 

\input{tables/pml_sigma_sensitivity_table.tex}

The resulting relative $L_2$ errors are shown in
\cref{fig:pmlSigmaSensitivity}.  With the PML coefficients scaled using the
local sound speed of medium~2, small values of $|\sigma_{\max}|$ do not provide
sufficient attenuation before the truncated boundary.  The amplitude error
decreases from $3.51\times10^{-1}$ at
$\sigma_{\max}=10^4\,\mathrm{s}^{-1}$ to approximately
$9.1\times10^{-4}$ at both $2\times10^5\,\mathrm{s}^{-1}$ and
$5\times10^5\,\mathrm{s}^{-1}$.  The selected value
$\sigma_{\max}=5\times10^5\,\mathrm{s}^{-1}$ is used for the default
layered-interface validation because it gives stronger attenuation in the PML
without degrading the physical-domain error.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{figures/pml_sigma_sensitivity.png}%
    \caption{Layered-interface case: sensitivity of the directional tensor PML solution to the maximum damping coefficient.}
    \label{fig:pmlSigmaSensitivity}
\end{figure}

To assess mesh dependence at the selected damping strength, an additional
sweep is performed with
$\sigma_{\max}=5.0\times10^5\,\mathrm{s^{-1}}$ while varying the number of
cells in the propagation direction.  The results in
\cref{tab:pmlMeshSensitivity,fig:pmlMeshSensitivity} quantify convergence of
the real pressure, imaginary pressure, and pressure amplitude for the
directional, medium-dependent PML formulation.  The individual component
errors show small non-monotonic variations because phase error redistributes
between the real and imaginary components.  Nevertheless, the amplitude error
decreases from $1.72\times10^{-2}$ at $N_x=500$ to
$9.13\times10^{-4}$ at $N_x=8000$, demonstrating convergence of the pressure
amplitude.

\input{tables/pml_mesh_sensitivity_table.tex}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{figures/pml_mesh_sensitivity.png}%
    \caption{Layered-interface case: directional tensor PML mesh sensitivity with fixed damping $\sigma_{\max}=5.0\times10^5\,\mathrm{s^{-1}}$.}
    \label{fig:pmlMeshSensitivity}
\end{figure}

\FloatBarrier
\section{Application example}
\label{sec:application}

\subsection{Height sweep in a single-axis acoustic levitator}
The motivation for developing the frequency-domain solver is to simulate the acoustic field around liquid drops in a single-axis acoustic levitator.  As an application example, we consider the height-sweep configuration of Andrade and Marzo \cite{andrade2019numerical}.  Their COMSOL study evaluates the radiation force on the reflector while varying the transducer-reflector distance $H$ for an empty levitator and for constant-volume drops represented by spheroids with different aspect ratios.

The OpenFOAM case \texttt{WedgeLevitatorDropAlpha} follows the same workflow, with the height sweep executed by the script \texttt{runHeightSweep.sh} in that case directory.  A one-degree wedge sector is used and the resulting force is scaled by the axisymmetric factor $360^\circ/1^\circ$.  The acoustic frequency is $25.25\,\mathrm{kHz}$, the transducer velocity amplitude is $1\,\mathrm{m\,s^{-1}}$, and the equivalent spherical drop radius is $1\,\mathrm{mm}$.  The drop is placed at the mid-height of the cavity and is represented by the volume-fraction field \texttt{alpha.water}; for the non-spherical cases the spheroid volume is kept constant while the aspect ratio $a/b$ is varied.  After solving the Helmholtz problem, the reflector force is obtained by integrating the acoustic radiation-pressure field, computed from \cref{eq:acousticRadiationPressure}, over the reflector patch,
\begin{equation}
    F_n = \int_{\Gamma_r} p_{\mathrm{rad}} \, \normal \cdot \normal_r \, \mathrm{d}S ,
\end{equation}
where $\Gamma_r$ is the reflector surface and $\normal_r$ is the mean reflector normal.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{figures/4sweep2.png}
    \caption{Application example: reflector radiation force during a height sweep of the wedge levitator for an empty cavity and constant-volume drops with different aspect ratios, computed with \texttt{acousticHelmholtzFoam}.}
    \label{fig:heightSweepAspectRatio}
\end{figure}

\cref{fig:heightSweepAspectRatio} shows the resulting force curves.  They reproduce the main trends reported in the COMSOL study of Andrade and Marzo \cite{andrade2019numerical}: the force is strongly amplified near the acoustic resonance, the drop shifts the resonance response, and the force level near the peak increases systematically with the drop aspect ratio.  Because the levitator operates close to resonance, the radiation force is highly sensitive to small changes in geometry, material parameters, acoustic drive, and numerical representation of the drop.  The agreement in resonance scale, curve ordering, and force magnitude is therefore a meaningful application-level comparison.  This example demonstrates that the solver can use the same interface-resolved material description required for two-phase flow simulations while producing acoustic-force trends consistent with the established finite-element reference.

\FloatBarrier
\section{Performance}
\label{sec:performance}
Performance was assessed with the \texttt{WedgeLevitatorDropAlpha} application case, which exercises the same heterogeneous two-phase coefficients, PML treatment, and radiation-pressure post-processing used in the height-sweep example.  The timings were collected on the workstation \texttt{legion5}, equipped with an AMD Ryzen 7 4800H processor with 8 physical cores and 16 hardware threads, using OpenFOAM v2312.  Each reported value is the elapsed wall-clock time for one \texttt{acousticHelmholtzFoam} solve after mesh generation and, for MPI runs, after domain decomposition.  The solver assembles the real-imaginary block system with two unknowns per finite-volume cell and solves it with PETSc using \texttt{-ksp\_type preonly}, \texttt{-pc\_type lu}, and the MUMPS sparse direct factorisation backend.  The MPI cases used Scotch decomposition.  A single unreported warm-up solve was performed before recording the benchmark data.

\begin{table}[H]
    \centering
    \caption{\texttt{WedgeLevitatorDropAlpha} strong-scaling timings for the $570\,818$-cell mesh, corresponding to $1\,141\,636$ block-system unknowns.}
    \label{tab:dropAlphaStrongScaling}
    \begin{tabular}{rrrr}
        \toprule
        MPI ranks & Elapsed time [s] & Speedup & Parallel efficiency [\%] \\
        \midrule
        1 & 23.81 & 1.00 & 100.0 \\
        2 & 15.58 & 1.53 & 76.4 \\
        4 & 11.13 & 2.14 & 53.5 \\
        8 & 10.02 & 2.38 & 29.7 \\
        \bottomrule
    \end{tabular}
\end{table}

\cref{tab:dropAlphaStrongScaling,fig:dropAlphaStrongScaling} show the strong-scaling behaviour for the finest benchmark mesh.  Increasing the rank count from one to eight reduces the elapsed solve time from $23.81\,\mathrm{s}$ to $10.02\,\mathrm{s}$, giving a speedup of $2.38$.  Most of the improvement is obtained between one and four ranks.  The additional reduction from four to eight ranks is modest, and the parallel efficiency decreases to $29.7\%$, indicating that this problem size is already too small to keep eight MUMPS factorisation processes efficiently occupied on the workstation.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figures/dropalpha_strong_scaling.png}
    \caption{\texttt{WedgeLevitatorDropAlpha} strong scaling for the $570\,818$-cell mesh.  The block system contains $1\,141\,636$ unknowns because the real and imaginary pressure components are solved in one coupled PETSc system.}
    \label{fig:dropAlphaStrongScaling}
\end{figure}

\cref{fig:dropAlphaProblemSizeScaling} reports the fixed-rank problem-size study.  With four MPI ranks, decreasing the cfMesh maximum cell size from $8.0\times10^{-5}\,\mathrm{m}$ to $3.0\times10^{-5}\,\mathrm{m}$ increases the mesh from $80\,569$ to $570\,818$ cells and the block system from $161\,138$ to $1\,141\,636$ unknowns.  Over this range the measured elapsed time increases from $1.75\,\mathrm{s}$ to $11.24\,\mathrm{s}$.  Thus the unknown count increases by a factor of $7.08$ while the solve time increases by a factor of $6.42$; a log-log fit gives an empirical exponent of approximately $0.95$.  The corresponding cost per unknown remains nearly constant, between $8.84\times10^{-6}$ and $1.09\times10^{-5}\,\mathrm{s}$ per unknown.  For these moderately sized axisymmetric wedge meshes, the assembled PETSc/MUMPS workflow therefore shows close to linear growth in wall-clock time with problem size when the rank count is fixed.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.92\textwidth]{figures/dropalpha_problem_size_scaling.png}
    \caption{\texttt{WedgeLevitatorDropAlpha} problem-size scaling at four MPI ranks.  The mesh size is controlled by the cfMesh maximum cell size, and the plotted unknown count is twice the cell count.}
    \label{fig:dropAlphaProblemSizeScaling}
\end{figure}

\section{Conclusions}

\label{sec:conclusions}

This paper presented \texttt{acousticHelmholtzFoam}, a parallel finite-volume
solver for time-harmonic acoustic wave propagation in heterogeneous two-phase
media.  The solver is implemented in OpenFOAM and solves the complex Helmholtz
equation by separating the pressure into coupled real and imaginary fields.
This formulation allows the variable-density acoustic operator, PML terms, and
two-phase material coefficients to be assembled with standard finite-volume
operators, while the resulting block-coupled system is solved through PETSc
with the MUMPS sparse direct solver.  The accompanying \texttt{setPMLFields}
utility separates the geometric construction of the PML damping tensor from the
solver-local construction of the PML coefficient fields, which keeps the case
setup explicit and supports both rectangular and spherical absorbing layers.

The verification cases demonstrate the main numerical capabilities required by
the intended applications.  For baffled-piston radiation, the computed
near-field pressure amplitude converges toward the analytical on-axis solution,
and the far-field directivity reconstructed from the numerical solution follows
the analytical piston pattern.  The layered-interface case verifies propagation
through a strong acoustic-impedance jump and the directional tensor PML in a
setting with an analytical reference.  In this case, the solver reproduces the
real and imaginary pressure components in the physical domain with relative
$L_2$ errors of $1.24\times10^{-3}$ and $1.54\times10^{-3}$, respectively, and
the pressure amplitude in the PML with a relative error of
$1.01\times10^{-5}$.  The damping and mesh-sensitivity studies further show
that the PML performance depends on a consistent choice of damping strength,
layer thickness, and resolution.

The single-axis levitator example illustrates how the solver can be used with
interface-resolved material fields for acoustofluidic applications.  Using the
volume-fraction field to represent constant-volume spheroidal drops, the
computed height-sweep force curves reproduce the principal trends reported in a
finite-element reference study: resonance amplification, resonance shifts due
to the drop, and increasing peak force with increasing drop aspect ratio.  This
demonstrates that the frequency-domain acoustic solve can be coupled naturally
to two-phase finite-volume workflows and can provide derived quantities such as
acoustic velocity, radiation pressure, and momentum flux for subsequent force
analysis.

The performance measurements confirm that the current PETSc/MUMPS
implementation is practical for moderately sized three-dimensional wedge cases.
For the $570\,818$-cell levitator mesh, the solve time decreased from
$23.81\,\mathrm{s}$ on one MPI rank to $10.02\,\mathrm{s}$ on eight ranks.  At
fixed rank count, the wall-clock time grew nearly linearly with the number of
block unknowns over the tested mesh range.  The limited strong-scaling
efficiency at the highest rank count is consistent with the cost structure of a
parallel sparse direct factorisation on a workstation-scale problem.

Future work will focus on extending the validation to fully three-dimensional
two-phase configurations, improving scalability for larger meshes through
iterative or hybrid linear solvers, and coupling the computed acoustic fields
more tightly to time-dependent interface-resolved flow simulations.  These
extensions would enable predictive simulations in which droplet deformation,
material-property jumps, acoustic resonance, and radiation forces are evolved
within a single finite-volume framework.


\bibliographystyle{elsarticle-num} 
\bibliography{bibliography}

\end{document}
