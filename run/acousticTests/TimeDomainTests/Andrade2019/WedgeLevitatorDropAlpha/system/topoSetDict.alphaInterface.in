FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      topoSetDict;
}

actions
(
    {
        name    alphaInterfaceCells;
        type    cellSet;
        action  new;
        source  fieldToCell;
        field   alpha.water;
        min     0;
        max     1;
    }
    {
        name    alphaInterfaceCells;
        type    cellSet;
        action  subtract;
        source  fieldToCell;
        field   alpha.water;
        min     0;
        max     0;
    }
    {
        name    alphaInterfaceCells;
        type    cellSet;
        action  subtract;
        source  fieldToCell;
        field   alpha.water;
        min     1;
        max     1;
    }
    {
        name    alphaInterfaceCells;
        type    cellSet;
        action  add;
        source  haloToCell;
        steps   __ALPHA_INTERFACE_ADJACENT_LAYERS__;
    }
);

// ************************************************************************* //
